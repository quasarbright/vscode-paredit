/**
 * Test utilities for paredit operations
 * Provides cursor notation format for easy test writing
 */

import { parseCursorString, formatCursorString } from './integration/cursor-notation';

// Re-export cursor notation utilities for backward compatibility
export { parseCursorString, formatCursorString };
